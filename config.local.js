// 배포 환경 설정.
//
// 이 파일은 커밋한다. 비밀이 하나도 없기 때문이다 — OpenAI 키는 서버(api/chat.js)의
// 환경변수에만 있고, 여기에는 "프록시를 쓴다"는 사실과 그 경로뿐이다.
//
// 커밋하지 않았을 때 무슨 일이 있었는지 남겨둔다:
// .gitignore 에 있던 동안에는 CLI 배포(vercel --prod)가 로컬 파일을 함께 올려
// 아무 문제가 없었다. 그런데 GitHub 연동으로 바꾸자 빌드가 레포에서만 파일을
// 받으면서 이 파일이 사라졌고, index.html 의 onerror 폴백이 mode:"rules" 로
// 떨어져 화면 전체가 "오프라인 모드"가 됐다. 조회도 실행도 전부 죽었다.
//
// 정적 파일과 api/ 가 같은 배포에 있으므로 같은 출처다 → 상대 경로면 충분하다.
window.KB_CONFIG = {
  mode: "proxy",
  proxyUrl: "/api",
};
